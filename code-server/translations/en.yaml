---
configuration:
  log_level:
    name: Log Level
    description: >-
      Defines the log level for add-on and Code Server service.
  init_path:
    name: Init script path
    description: >-
      Defines the path for custom-config and custom-service scripts.
      Deactivated if config option is empty.
